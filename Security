var password = prompt("Daily access token required.");
if(password != 'Jakel'){
document.body.innerHTML = '';
document.body.innerHTML = 'You have been denied access';
window.close();
}else{
alert('Access Granted.');
}
